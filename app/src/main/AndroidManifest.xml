<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bingewatchers" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BingeWatchers" >
        <activity android:name=".Activity_profile" >
        </activity>
        <activity android:name=".MovieInfo" />
        <activity android:name=".GroupInfo" />
        <activity android:name=".ChatWindow" />
        <activity android:name=".SplashScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateJoinGroup" />
        <activity android:name=".MainActivity" />
        <activity android:name=".GenreSelection" />
        <activity android:name=".SignUp" />
        <activity android:name=".DashBoard" />
    </application>

</manifest>